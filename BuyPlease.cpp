#include <iostream>
using namespace std;

int main()
{
    // your code goes here
    int a, b, x, y, total;
    cin >> a >> b >> x >> y;
    total = a * x + b * y;
    cout << total << endl;
    return 0;
}